/**
 * Host half of the quick model widget.
 * The feature is browser-only; the host row exists so DSH can compose the
 * package and discover its dsh.client bundle.
 */
function apply() {}

export { apply };
